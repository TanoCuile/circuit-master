{% extends "::layout.html.twig" %}

{% block content %}
    {% for group in groups %}
        <div class="box">
            <header>
                <h5><a href="{{ path('group_members', { id:group.id }) }}">{{ group.name }}</a></h5>
            </header>
        </div>
    {% endfor %}
{% endblock %}