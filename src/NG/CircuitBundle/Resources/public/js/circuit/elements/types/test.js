MasterApp.module('Circuit', function initializeTest(module, app, Backbone, Marionette, $, _){
    module.componentTypes['e1'] = module.ComponentController.extend({

    });
    module.componentTypes['e2'] = module.ComponentController.extend({

    });
    module.componentTypes['e3'] = module.ComponentController.extend({

    });
    module.componentTypes['e4'] = module.ComponentController.extend({

    });
    module.componentTypes['ml'] = module.ComponentController.extend({

    });
    module.componentTypes['mt'] = module.ComponentController.extend({

    });
    module.componentTypes['p'] = module.ComponentController.extend({

    });
    module.componentTypes['inductor'] = module.ComponentController.extend({

    });
    module.componentTypes['diod'] = module.ComponentController.extend({

    });
    module.componentTypes['hl'] = module.ComponentController.extend({

    });
    module.componentTypes['gl'] = module.ComponentController.extend({

    });
});